server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://188.131.240.160:33306/rbac_seurity?ssl=fasle
    username: root
    password: rbac_seurity

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.security.model
  configuration:
    mapUnderscoreToCamelCase: true


logging:
  level:
    org.springframework.web: INFO
    com.example.seurity.mapper: DEBUG